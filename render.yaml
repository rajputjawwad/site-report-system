services:
  - type: web
    name: site-report-system
    env: python
    plan: free
    buildCommand: bash render-build.sh && pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
